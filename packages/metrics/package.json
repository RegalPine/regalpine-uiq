{
  "name": "@uiq/metrics",
  "version": "1.0.0",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "dependencies": {
    "@uiq/core": "workspace:*",
    "@uiq/color": "workspace:*",
    "@uiq/geometry": "workspace:*",
    "@uiq/measurement": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --sourcemap --target es2022 --platform neutral",
    "typecheck": "tsc --noEmit"
  }
}
